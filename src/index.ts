// Explicit types

// String Type
let course: string = 'Next level development course';

// Number Type
const age: number = 75;

// Boolean Type
const isAdmin: boolean = true;

// Any type
let courses;
// Now this can be any type. That's its called any type. This is a bad practice.
let course2: string; // This is good practice
course2 = 'Next level Development';
